{
  "name": "puddin-pool-web-client",
  "version": "1.0.0",
  "private": true,
  "description": "Universal 120Hz WebGL2/Canvas Client & Capacitor Mobile Wrapper for Puddin's Pool",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sync:mobile": "vite build && npx cap copy && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/screen-orientation": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
